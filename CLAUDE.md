# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Node.js CommonJS project (`"type": "commonjs"`). Entry point: `index.js`. No dependencies or build tooling configured yet.

## Commands

```bash
# Install dependencies (when added)
npm install

# Run tests (not yet configured)
npm test
```

## Notes

- No test framework, linter, or bundler has been set up yet. Add relevant sections here as the project evolves.
